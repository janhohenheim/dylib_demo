[workspace]
members = ["host", "common", "plugin"]
#exclude = ["../plugin"]
resolver = "3"

[workspace.dependencies]
libloading = "0.9"
host = { version = "0.1", path = "host" }
common = { version = "0.1", path = "common" }
plugin = { version = "0.1", path = "plugin" }
